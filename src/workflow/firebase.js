/**
 * Firebase stub — Firebase is intentionally disabled in the Content Hub
 * prototype. All data is managed in-memory for prototyping/review purposes.
 * The real Firebase connection lives in the standalone agent-builder-main app.
 */
export const db = null;
